define(function(require){
    'use strict';
    var Backbone = require('backbone');

    var SearchResult = Backbone.Model.extend({
        defaults: {

        },
        initialize: function(){

        }
    });

    return SearchResult;
});